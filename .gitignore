dist/
# The npm tarball and its staging directory: built by `deno task pack`, published by CI
# next to the site, never committed.
dist-npm/
.certs/
.claude/
node_modules/
.mcp.json
/bench/
# Marks: what the `mark` switch posts back, local to one machine like bench results.
/marks/
# What sweden-election.ts fetched (election results, boundaries); the generated
# src/worlds/sweden-data.wgsl is what is committed.
/data/
